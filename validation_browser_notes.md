# Browser Validation Notes

時間：2026-05-15 02:14（GMT+8）

瀏覽器開啟預覽網址後，BTCUSDT Dashboard 主頁可正常載入。頁面顯示 TradingView/K 線主區、左側工具列、右側分頁入口（策略、指標、SMC、PA、纏論、新聞）與下方面板。右下角行情狀態目前顯示「行情即時」，代表 WebSocket/ticker 更新已恢復，降級提示橫幅因即時狀態正常而未顯示。頁面內容成功取得 V8 策略資料、共識評分、入場參數與風險資訊。

需補充驗證：點擊「新聞」分頁檢查新聞錯誤/空資料狀態；點擊「纏論」分頁確認 chan_mtf 四時區資料不再為空。

## 纏論分頁驗證

點擊右側「纏論」分頁後，頁面成功顯示多時段纏論總結，包含整體趨勢共識、看多/看空/震盪數量、趨勢一致性、主導時段、入場時機與 4H/1H/15M/5M 快速一覽入口。這代表 `chan_mtf.timeframes` 與 `chan_mtf.signals` 已不再是空物件，前端纏論面板可讀取正式資料契約。

## 乾淨重啟後最終纏論驗證

清理舊行程後以 3001 重新啟動服務，再次刷新預覽頁並進入「纏論」分頁。最新版本成功顯示 4H、1H、15M、5M 四時區快速一覽，其中 4H 與 1H 為上升偏多並標示買入，15M 為震盪中性，5M 為下降偏空並標示賣出；摘要顯示看多 2/4、震盪 1/4、看空 1/4，主導時段 4h，詳細文字為「目前使用穩定版本地引擎計算多時區纏論結構與趨勢共振」。這確認後端 `chan_mtf` 正式契約已由 PA 內嵌纏論資料補齊，並非舊版空物件。

行情狀態仍顯示「行情即時」，降級提示橫幅在即時連線正常時正確隱藏。

## 新聞分頁驗證

切換至「新聞」分頁後，初始狀態顯示「最新資訊／正在讀取 RSS 來源」與骨架載入列；資料完成載入後顯示情緒統計、過去 24 小時新聞密度、關鍵字篩選、情緒篩選與新聞列表。此次驗證取得 40 則新聞，看多 10、看空 5、中性 25，代表正常資料情境沒有回歸問題。若資料源異常或空資料，已在程式中加入重試按鈕與清楚說明。

## API 契約驗證

使用本機 `/api/trpc/crypto.getSnapshot` 查詢 `BTCUSDT` 後，JSON 回應中 `chan_mtf.timeframes`、`chan_mtf.signals` 與 `chan_mtf.summary` 均存在，且 `timeframes` 與 `signals` 皆包含 `4h`、`1h`、`15m`、`5m` 四個鍵值。這與前端纏論分頁呈現一致。
