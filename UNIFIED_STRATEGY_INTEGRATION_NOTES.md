# 統一策略整合設計備忘

目前策略相關功能主要分散於 Dashboard 的 `strategy`、`consensus`、`forecast`、`highwinrate`、`cannonball`、`panda`、`combo`、`signal_alert`、`champion_analysis` 等分頁。此次整合目標不是刪除原有策略演算法或 API，而是先把使用者操作入口整合成一個「統一策略中心」，將相同或高度重疊的策略面板收斂在同一頁內，避免使用者必須在多個分頁之間切換。

## 整合邊界

| 類別 | 原分散位置 | 新呈現方式 |
|---|---|---|
| 核心交易決策 | 策略建議、共識評分、預測情境 | 統一放在策略中心最上層，作為主要決策區 |
| 高勝率/組合策略 | 高勝率模型、Combo 策略、CannonBall、熊貓策略、冠軍分析 | 統一放在同一頁的「高勝率與專家模型」區塊 |
| 執行與告警 | 即時訊號、交易日誌、回測 | 統一放在「執行、告警與驗證」區塊 |
| 舊有分頁 | 多個策略子分頁 | 從主要導覽移除，保留程式碼相容與必要 fallback |

## 實作策略

本次以低風險方式建立新的 `UnifiedStrategyCenterPanel`，並在 Dashboard 的 `strategy` 分頁渲染。原本獨立策略元件仍保留並被統一中心引用，確保後端 API、快取、狀態與既有元件邏輯不需要大幅重寫。Dashboard 導覽會移除重複策略子分頁，降低分散感；若未來需要，可再進一步把後端策略輸出結構也整合成單一 API。


## 驗證結果（2026-05-13）

前端建置已使用 `pnpm exec vite build` 驗證通過，Dashboard 已重新啟動於 `http://localhost:3000/`。公開連結載入後，點擊「策略中心」僅顯示單一子分頁「統一策略」，畫面進入「統一策略中心」，並可看到核心交易決策、高勝率與專家模型、執行告警與驗證等整合區塊。瀏覽器主控台未顯示新增錯誤。

補充：`pnpm check` 仍會被既有伺服器檔案 `server/run_v4_five_strategy_live.ts:836` 的語法錯誤阻擋，該錯誤不屬於本次策略中心前端整合新增內容。
