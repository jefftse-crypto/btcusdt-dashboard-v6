# BTCUSDT Dashboard v6：亮色手機版與專業指標覆蓋更新

本次更新依據使用者提供的截圖與需求，集中處理兩個重點：第一，檢查 Dashboard 是否覆蓋 **VWAP、CVD、TPO、OI 與趨勢線** 等交易者常用指標；第二，將原本偏深色、在手機上觀感較壓迫的 Dashboard 調整為更明亮、更適合小螢幕閱讀的卡片式介面。

## 指標覆蓋狀態

| 指標 | 目前狀態 | Dashboard 呈現方式 | 後續建議 |
|---|---|---|---|
| VWAP | 已接入 | 主頁新增專業指標覆蓋卡，標示 VWAP 已接入；既有技術指標與 PA 相關入口可讀取 | 可進一步在 K 線圖上加入更醒目的 VWAP 圖層切換 |
| CVD | 待接資料源 | 主頁明確標示目前未接逐筆主動買賣量資料，不以假數據填充 | 建議新增 Binance aggTrades 或期貨成交流，建立主動買 / 主動賣累積量模型 |
| TPO | 部分覆蓋 | 目前已有 VPVR / Volume Profile 類功能，主頁標示為部分覆蓋 | 若需要完整 Market Profile / TPO，需新增時間價格機會分佈模型 |
| OI | 已接入 | 主頁標示 OI 已接入，並指向鏈上 / 衍生品入口 | 可新增 OI 變化率與價格背離提示 |
| 趨勢線 | 結構替代 | 目前由支撐阻力、SMC、PA 水位與多時間框架結構承擔 | 可新增手動畫線或自動趨勢線圖層 |

## 視覺與手機版更新

本次已新增 `dashboard-light-theme` 全域覆寫樣式，將 Dashboard 從大面積黑底調整為淺灰白卡片式介面，改善手機螢幕上的可讀性。頂部導覽、卡片背景、底部手機導覽、文字對比與觸控區塊也做了同步優化。

## 已修改主要檔案

| 檔案 | 修改內容 |
|---|---|
| `client/src/pages/Dashboard.tsx` | 新增專業指標覆蓋摘要卡，並套用亮色 Dashboard 根樣式 |
| `client/src/index.css` | 新增亮色主題與手機版覆寫樣式 |
| `DASHBOARD_FULL_REVIEW_NOTES.md` | 補充本輪檢查、修改與驗證紀錄 |

## 驗證結果

已執行 `pnpm exec vite build`，前端建置成功。公開 Dashboard 頁面已重新啟動並驗證可正常載入，首頁已顯示「專業指標覆蓋」卡，並且瀏覽器主控台沒有新增前端錯誤。

## 啟動方式

請在專案根目錄執行以下指令：

```bash
pnpm install
pnpm dev
```

若要建立前端生產建置，可執行：

```bash
pnpm exec vite build
```

完整 `pnpm build` 仍可能受既有伺服器端舊檔案語法問題影響；本次前端修改已透過 Vite 建置驗證通過。
